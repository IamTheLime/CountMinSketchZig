const std = @import("std");
const Allocator = @import("std").mem.Allocator;

const CountMinSketchCell = struct {
    elements: std.BufSet,
    count_distinct: u32,
};

fn CountMinSketch(comptime hash_count: u8, buckets: u16) type {
    return struct {};
}

test "Count min sketch test" {}
